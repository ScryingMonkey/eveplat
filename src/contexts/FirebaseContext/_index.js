import FirebaseContext from "./FirebaseContext";
import Firebase from "./Firebase";
import firebaseConfig from './firebaseConfig';

export default Firebase;
export { FirebaseContext, firebaseConfig };
